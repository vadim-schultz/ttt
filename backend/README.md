Table tennis tournament
-----------------------

This is a web app to run table tennis tournaments.

Deployment
----------
Run docker compose from docker/ directory to spin up the containers:

```console
docker compose up
```  
